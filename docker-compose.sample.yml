version: "3.9"
services:
  toogoodtogo_notify:
    build: .
    #environment:
    #  - TELEGRAM_TOKEN="ENTER TOKEN HERE"
    #  - EMAIL="ENTER TOOGOODTOGO EMAIL HERE"
    restart: unless-stopped
    volumes:
      - data:/app/data
volumes:
  data: